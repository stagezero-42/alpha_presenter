# app/utils.py
# This file can contain utility functions, constants, or helper classes
# used across different modules of the application.

# Example Constant:
# DEFAULT_IMAGE_DURATION_MS = 5000

# Example Utility Function:
# def format_time_ms(milliseconds):
#     """Formats milliseconds into MM:SS or HH:MM:SS string."""
#     if milliseconds < 0: milliseconds = 0
#     seconds_total = milliseconds // 1000
#
#     hours = seconds_total // 3600
#     minutes = (seconds_total % 3600) // 60
#     seconds = seconds_total % 60
#
#     if hours > 0:
#         return f"{hours:02d}:{minutes:02d}:{seconds:02d}"
#     else:
#         return f"{minutes:02d}:{seconds:02d}"

print("app.utils initialized")
